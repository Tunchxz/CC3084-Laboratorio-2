# Laboratorio 2 — Deep Learning (CC3084)
# Instalar en un entorno virtual propio del laboratorio:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt

# PyTorch en versión CPU. La serie tiene 210 observaciones y los modelos son de
# una o dos capas, así que la GPU no aporta nada y el wheel CPU pesa ~250 MB en
# lugar de los ~3 GB del wheel con el stack CUDA completo.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.9

# Extracción de características de series de tiempo (Ejercicio 2)
pycatch22>=0.4.5

# Análisis de datos y visualización
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13

# PCA, clustering, estandarización y métricas
scikit-learn>=1.3
scipy>=1.11

# Descomposición y ACF, para contrastar catch22 contra el análisis del Lab 1
statsmodels>=0.14

# Entorno de notebooks
jupyter>=1.0
ipykernel>=6.29
