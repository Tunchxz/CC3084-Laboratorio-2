# Laboratorio 2 — Deep Learning (LSTM) y catch22 (CC3084)
# Instalar en un entorno virtual dedicado:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
pandas>=2.2
numpy>=1.26,<2.2
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
tensorflow>=2.16,<2.20
pycatch22>=0.4
ipykernel>=6.29
jupyter>=1.0
jinja2>=3.1.5

# Nota: numpy se fija por debajo de 2.2 por compatibilidad de ABI con
# TensorFlow. Si `import tensorflow` falla tras instalar, revisar primero
# la versión de numpy antes de investigar otra causa (ver CLAUDE.md, sección 7).
